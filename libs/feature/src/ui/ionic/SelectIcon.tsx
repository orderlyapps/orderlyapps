import { IonIcon } from '@ionic/react';
import { checkmarkCircle } from 'ionicons/icons';

export const SelectIcon = () => {
  return <IonIcon icon={checkmarkCircle} color="primary" slot="end" />;
};
