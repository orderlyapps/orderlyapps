# Proclaimer Package — TODO

- [ ] **Placeholder metadata** — `packages/proclaimer/package.json` (and all other packages: `packages/utils/package.json`, `packages/ui/ionic/package.json`, `tools/config/package.json`) still have placeholder values for `description`, `author`, `homepage`, `bugs.url`, and `repository.url`. Replace with real project metadata.
- [ ] **Deep relative import paths** — `create-publishers-collection.ts` uses `../../../supabase/get-client-id.js` to import `getClientId`. As more collections adopt this utility, consider adding a path alias to improve readability instead of relying on deeply nested relative paths.
- [ ] **Search filter format mismatch in `publisher-select-list.tsx`** — `PublisherSelectList` filters using `formatPublisherName(p)` (default format → "Ada Lovelace") but items are displayed with `format="last_name, display_name"` (→ "Lovelace, Ada"). Searching for text as it appears on screen (e.g. "lovelace, a") returns no results. The search should use the same format as the display.
- [ ] **Inconsistent name format in `family-detail-row.tsx`** — `FamilyDetailRow` renders the family head's name with `formatPublisherName(head)` (default format → "Ada Lovelace") while family members are rendered via `PublisherListItem` which uses `format="last_name, display_name"` (→ "Lovelace, Ada"). The head and members are shown with different name formats in the same component.

- [ ] **`name-detail-row.tsx` exceeds 75-line limit** — `packages/proclaimer/src/feature/publishers/components/publisher-details/components/name-detail-row/name-detail-row.tsx` is 111 lines. The `IonAlert` configuration with 4 inputs and validation logic is the bulk. Consider extracting the alert into a `name-edit-alert/` subfolder component.
- [ ] **`publisher-select-list.tsx` exceeds 75-line limit** — `packages/proclaimer/src/feature/publishers/components/publisher-select-modal/components/publisher-select-list/publisher-select-list.tsx` is 88 lines. Consider extracting the search/filter logic into a hook.

- [ ] **`useUpdatePublisher` — `onError` callback exceptions are silently swallowed** — In `packages/proclaimer/src/feature/publishers/hooks/use-update-publisher.ts`, the `.catch()` handler calls `onError(toError(reason))` inside a `void`-ed promise. If `onError` itself throws synchronously, the rejection becomes an unhandled promise rejection. Consider wrapping the callback invocation in a try/catch.
- [ ] **`usePublishers` — `error` is `null` when `isError` is `true`** — In `packages/proclaimer/src/feature/publishers/hooks/use-publishers.ts`, `buildPublishersResult` derives `error` from `publishers.utils.lastError`, but `isError` also reflects the `useLiveQuery` error. When the live query errors but `lastError` is unset or has been reset, `hasError` is `true` yet `error` resolves to `null`. Consumers fall back to their generic message (e.g. "Failed to load publishers") and the actual error detail is lost. Consider surfacing the `useLiveQuery` error as a fallback when `lastError` is unavailable.
- [ ] **`publisher-details.tsx` uses `describeSupabaseError` instead of `describePublisherError`** — `packages/proclaimer/src/feature/publishers/components/publisher-details/publisher-details.tsx` imports `describeSupabaseError` from `@amodeo/utils/supabase` for load errors, while its child components `NameDetailRow` and `FamilyDetailRow` use `describePublisherError` from `publisher-errors.ts` for update errors. `describePublisherError` delegates to `describeSupabaseError` for non-publisher-specific cases, so using it in the parent would be a strict improvement — it adds publisher-specific messages (e.g. 23503 FK on `family_id` → "The selected family or group no longer exists.") without losing any existing behavior.

## Utils Package — TODO

- [ ] **`@amodeo/utils` main export missing `development` condition** — `packages/utils/package.json` defines `development` and `types@development` conditions for the `./supabase` subpath but not for the main `.` export. This means imports from `@amodeo/utils` (e.g. `toError`, `getErrorMessage`) resolve to built dist during development while `@amodeo/utils/supabase` resolves to source. If the dist is stale, main-entry imports may fail. Consider adding `"development": "./src/index.ts"` and `"types@development": "./src/index.ts"` to the `.` export.

## UI/Ionic Package — TODO

- [ ] **`Body` component — `IonicColor` vs Ionic's `Color` type** — `packages/utils/src/vendor/ionic/types/ionic-color.ts` restricts colors to 9 predefined names. Ionic's built-in `Color` also accepts arbitrary custom color names registered in theme CSS. If consumers define custom Ionic colors they will be blocked by this strict type. Additionally, duplicating a vendor type in `@amodeo/utils` means it can drift from future Ionic versions. Consider re-exporting Ionic's `Color` type instead.
- [ ] **`Body` component — `balance` and `pretty` mutual exclusivity** — If both `balance` and `pretty` props are `true`, `balance` silently wins (`body.tsx:59-63`). Consider making the mutual exclusivity explicit at the type level, e.g. a union prop like `wrap?: "balance" | "pretty"`.
