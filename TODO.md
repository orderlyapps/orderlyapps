## Proclaimer Package — TODO

- [ ] **Placeholder metadata** — `packages/proclaimer/package.json` (and all other packages: `packages/utils/package.json`, `packages/ui/ionic/package.json`, `tools/config/package.json`) still have placeholder values for `description`, `author`, `homepage`, `bugs.url`, and `repository.url`. Replace with real project metadata.
- [ ] **Deep relative import paths** — `create-publishers-collection.ts` uses `../../../supabase/get-client-id.js` to import `getClientId`. As more collections adopt this utility, consider adding a path alias to improve readability instead of relying on deeply nested relative paths.
- [ ] **Search filter format mismatch in `publisher-select-list.tsx`** — `PublisherSelectList` filters using `formatPublisherName(p)` (default format → "Ada Lovelace") but items are displayed with `format="last_name, display_name"` (→ "Lovelace, Ada"). Searching for text as it appears on screen (e.g. "lovelace, a") returns no results. The search should use the same format as the display.
- [ ] **Inconsistent name format in `family-detail-row.tsx`** — `FamilyDetailRow` renders the family head's name with `formatPublisherName(head)` (default format → "Ada Lovelace") while family members are rendered via `PublisherListItem` which uses `format="last_name, display_name"` (→ "Lovelace, Ada"). The head and members are shown with different name formats in the same component.

- [ ] **`name-detail-row.tsx` exceeds 75-line limit** — `packages/proclaimer/src/feature/publishers/components/publisher-details/components/name-detail-row/name-detail-row.tsx` is 111 lines. The `IonAlert` configuration with 4 inputs and validation logic is the bulk. Consider extracting the alert into a `name-edit-alert/` subfolder component.
- [ ] **`publisher-select-list.tsx` exceeds 75-line limit** — `packages/proclaimer/src/feature/publishers/components/publisher-select-modal/components/publisher-select-list/publisher-select-list.tsx` is 88 lines. Consider extracting the search/filter logic into a hook.

- [ ] **`useUpdatePublisher` — `onError` callback exceptions are silently swallowed** — In `packages/proclaimer/src/feature/publishers/hooks/use-update-publisher.ts`, the `.catch()` handler calls `onError(toError(reason))` inside a `void`-ed promise. If `onError` itself throws synchronously, the rejection becomes an unhandled promise rejection. Consider wrapping the callback invocation in a try/catch.
- [ ] **`usePublishers` — `error` is `null` when `isError` is `true`** — In `packages/proclaimer/src/feature/publishers/hooks/use-publishers.ts`, `buildPublishersResult` derives `error` from `publishers.utils.lastError`, but `isError` also reflects the `useLiveQuery` error. When the live query errors but `lastError` is unset or has been reset, `hasError` is `true` yet `error` resolves to `null`. Consumers fall back to their generic message (e.g. "Failed to load publishers") and the actual error detail is lost. Consider surfacing the `useLiveQuery` error as a fallback when `lastError` is unavailable.
- [ ] **`publisher-details.tsx` uses `describeSupabaseError` instead of `describePublisherError`** — `packages/proclaimer/src/feature/publishers/components/publisher-details/publisher-details.tsx` imports `describeSupabaseError` from `@amodeo/utils/supabase` for load errors, while its child components `NameDetailRow` and `FamilyDetailRow` use `describePublisherError` from `publisher-errors.ts` for update errors. `describePublisherError` delegates to `describeSupabaseError` for non-publisher-specific cases, so using it in the parent would be a strict improvement — it adds publisher-specific messages (e.g. 23503 FK on `family_id` → "The selected family or group no longer exists.") without losing any existing behavior.

- [ ] **Auth step — verify email convention `{publisher.id}@proclaimer.app`** — `packages/proclaimer/src/feature/auth/components/auth-step/auth-step.tsx:16` derives the Supabase auth email from `publisher.id` (the DB row UUID), not `auth_id` (the Supabase auth user UUID). Confirm that auth accounts were created with `{publisher.id}@proclaimer.app` as the email; otherwise sign-in will fail silently.
- [ ] **Auth step — Back button active during in-flight auth requests** — `packages/proclaimer/src/feature/auth/components/auth-step/auth-step.tsx:48-50`: the `loading` state lives inside `PasswordForm` / `OtpForm`, so `AuthStep`'s Back button is never disabled. A user can navigate back mid-auth, orphaning the pending Supabase call and triggering a state update on an unmounted component. Consider lifting `loading` state up to `AuthStep` or accepting an `isBusy` callback from the forms.
- [ ] **Auth step — `onSuccess` type should be `() => void | Promise<void>`** — `packages/proclaimer/src/feature/auth/components/auth-step/auth-step.tsx:8-12`: `AuthStepProps.onSuccess` is typed as `() => void` but the forms declare `onSuccess: () => void | Promise<void>` and `await` it. The type should match the contract the forms rely on.
- [ ] **OTP form — "Resend Code" doesn't clear stale `code`/`error`** — `packages/proclaimer/src/feature/auth/components/auth-step/components/otp-form/otp-form.tsx:122-124`: clicking "Resend Code" returns to the request phase but leaves `code` and `error` populated. After requesting a new code, the old invalid code is still in the input and any previous error is still displayed. Should reset both on click.
- [ ] **Password form — `onSuccess` failure misattributed as auth error** — `packages/proclaimer/src/feature/auth/components/auth-step/components/password-form/password-form.tsx:28-29`: if `onSuccess` (which calls `setMany`) throws, the error is caught by the surrounding `catch` and shown as a generic auth error. The user has already authenticated but sees an error, and re-clicking "Sign In" re-attempts `signInWithPassword` which may error with "already authenticated". Consider calling `onSuccess()` outside the try/catch or separating auth errors from persistence errors.
- [ ] **Onboarding flow — `handleAuthSuccess` silent no-op if `selectedPublisher` is null** — `packages/proclaimer/src/feature/onboarding/components/onboarding-flow/onboarding-flow.tsx:44-47`: the guard returns silently if `selectedPublisher` is null. If it ever fires, the user has authenticated with no onboarding completion — a stuck state with no error feedback. Low risk given the render guard, but worth noting.

- [ ] **`useUpdatePermission` — `UpdatePermissionChanges` allows updating server-managed timestamp fields** — `packages/proclaimer/src/feature/permissions/hooks/use-update-permission.ts:7-9`: `UpdatePermissionChanges` omits `auth_user_id` and `congregation_id` but still includes `created_at` and `updated_at`. Callers can accidentally update server-managed timestamps (e.g. `update("abc", "xyz", { created_at: "1970-01-01" })` compiles). The `onUpdate` handler uses `permissionRecordSchema.partial()` which accepts all fields, so these would be sent to Supabase. The type should also omit `created_at` and `updated_at`.
- [ ] **`usePermissions` — `like`/`ilike` operators allowed on non-string columns** — `packages/proclaimer/src/feature/permissions/hooks/use-permissions.ts:57-67`: The `PermissionFilter` type allows `PatternOperator` (`like`/`ilike`) on any column, including `can_edit` (boolean). This requires a cast at lines 172-174 and lets callers write nonsensical filters like `{ column: "can_edit", op: "like", value: "%foo%" }` that type-check but produce invalid SQL at runtime. The filter type should restrict `like`/`ilike` to string columns only.
- [ ] **`buildServiceYear` ignores `aux_pio` field** — `packages/proclaimer/src/feature/reports/utils/service-year.ts:40-43`: `auxiliary_pioneer` is inferred from whether `hours` is non-null rather than using the actual `aux_pio` boolean on the `Report` schema. Any report with hours from a non-regular/special pioneer will be incorrectly marked as auxiliary pioneer. Should use `r?.aux_pio ?? false`.
- [ ] **`other_sheep` and `anointed` hardcoded in `buildPublisherRecord`** — `packages/proclaimer/src/feature/reports/components/download-all-reports-button/hooks/use-all-reports-data.ts:45-46`: Every publisher is hardcoded as `other_sheep: true, anointed: false`. If anointed publishers exist, their PDF records will be incorrect. The publisher schema lacks these fields, so this needs a data source or schema change.
- [ ] **`is_field_missionary` checkbox will never be checked** — `packages/proclaimer/src/feature/reports/components/all-reports-pdf/components/service-year-report/service-year-report.tsx:31`: The publisher schema has no `field_missionary` type, so `publisher.type === "field_missionary"` is always false. Dead code that should be removed or wired to the correct data.

- [ ] **Backdrop flash on empty entries in `download-all-reports-button.tsx`** — `packages/proclaimer/src/feature/reports/pdf/download-all-reports-button/download-all-reports-button.tsx:26-27`: If `buildEntries` returns an empty array, the backdrop appears for ~2 frames (during the double rAF yield) then immediately disappears. Move the `buildEntries` + empty check before `set_is_generating(true)` to avoid the visual flash and unnecessary rAF yield.
- [ ] **Immediate `URL.revokeObjectURL` after `link.click()` in `download-all-reports-button.tsx`** — `packages/proclaimer/src/feature/reports/pdf/download-all-reports-button/download-all-reports-button.tsx:34-36`: Revoking the object URL synchronously after `link.click()` can cause download failures in some Safari versions where download initiation is not fully synchronous. Consider deferring `URL.revokeObjectURL` with a short `setTimeout`.
- [ ] **`DownloadBackdrop` is non-blocking despite visual overlay** — `packages/proclaimer/src/feature/reports/pdf/download-all-reports-button/components/download-backdrop/download-backdrop.tsx:20`: `IonBackdrop` has `tappable={false}` and the overlay div has `pointerEvents: "none"`, so users can still interact with UI beneath the backdrop during generation. If blocking interaction is intended, remove `tappable={false}` and set `pointerEvents: "auto"` on the overlay.
- [ ] **`getPublisherName` duplicated across components** — `getPublisherName` is identical in `missing-details-item.tsx` and `missing-details-modal.tsx`. Extract to a shared utility to avoid drift.
- [ ] **`MissingDetailsModal` — verify sub-components handle insert when `entry.local` is null** — `missing-details-modal.tsx` constructs a fallback `PublisherLocal` object when `entry.local` is `null`. `PublisherLocalDetails` is rendered without `read_only`, so sub-components (`PhoneList`, `AddressList`, etc.) may attempt database writes. Verify these components handle the insert case (no existing document) rather than only updates, since this modal is specifically for publishers with missing details.
- [ ] **`contacts-pdf-document.tsx` exceeds 75-line limit** — `packages/proclaimer/src/feature/publisher-local/components/download-contacts-pdf-button/components/contacts-pdf-document/contacts-pdf-document.tsx` is 165 lines. The `StyleSheet` and table rendering should be split into sub-components.
- [ ] **`download-contacts-pdf-button.tsx` exceeds 75-line limit** — `packages/proclaimer/src/feature/publisher-local/components/download-contacts-pdf-button/download-contacts-pdf-button.tsx` is 85 lines. Consider extracting the `handleDownload` logic into a custom hook (e.g. `use-download-pdf`).
- [ ] **Object URL leak on error in `download-contacts-pdf-button.tsx`** — In `handleDownload`, `URL.revokeObjectURL(url)` is inside the `try` block. If `link.click()` throws, the object URL is never revoked. Move `URL.revokeObjectURL` to a `finally` block to prevent the resource leak.

## Utils Package — TODO

- [ ] **`@amodeo/utils` main export missing `development` condition** — `packages/utils/package.json` defines `development` and `types@development` conditions for the `./supabase` subpath but not for the main `.` export. This means imports from `@amodeo/utils` (e.g. `toError`, `getErrorMessage`) resolve to built dist during development while `@amodeo/utils/supabase` resolves to source. If the dist is stale, main-entry imports may fail. Consider adding `"development": "./src/index.ts"` and `"types@development": "./src/index.ts"` to the `.` export.

## UI/Ionic Package — TODO

- [ ] **`Body` component — `IonicColor` vs Ionic's `Color` type** — `packages/utils/src/vendor/ionic/types/ionic-color.ts` restricts colors to 9 predefined names. Ionic's built-in `Color` also accepts arbitrary custom color names registered in theme CSS. If consumers define custom Ionic colors they will be blocked by this strict type. Additionally, duplicating a vendor type in `@amodeo/utils` means it can drift from future Ionic versions. Consider re-exporting Ionic's `Color` type instead.
- [ ] **`Body` component — `balance` and `pretty` mutual exclusivity** — If both `balance` and `pretty` props are `true`, `balance` silently wins (`body.tsx:59-63`). Consider making the mutual exclusivity explicit at the type level, e.g. a union prop like `wrap?: "balance" | "pretty"`.
- [ ] **Redundant `ref` declaration in input component props** — `TextInputProps`, `EmailInputProps`, `NumberInputProps`, and `SelectInputProps` all re-declare `ref?: Ref<HTMLIonInputElement>` (or `HTMLIonSelectElement`) in an intersection type that already includes it from the layout props. The `ref` is already present in `InputLayoutProps` / `SelectLayoutProps` via `ComponentProps<typeof IonInput/IonSelect>`. The redundancy is harmless but could be cleaned up by removing the explicit `ref` from each input props type.
